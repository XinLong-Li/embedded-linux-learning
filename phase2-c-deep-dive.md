# 阶段二：C 语言深度进修

> **时间：** 6-8 周 | **难度：** ⭐⭐☆☆☆ | **目标：** 写出健壮、高效的嵌入式 C 代码

---

## 📌 学习目标

- 彻底掌握指针（嵌入式 C 的核心）
- 熟练使用动态内存管理
- 理解数据结构在底层如何实现
- 掌握位操作技巧
- 理解编译、链接的底层细节
- 能写出可移植的嵌入式 C 代码
- 掌握 GDB 调试技巧

> ⚠️ 嵌入式 Linux 开发中，内核代码、驱动、Bootloader 几乎全是 C。C 语言水平直接决定了你的天花板。

---

## 📖 第1周：指针彻底搞懂

### Day 1-2：指针基础回顾
- [ ] 指针即地址，画内存图理解
- [ ] 指针声明、取地址 `&`、解引用 `*`
- [ ] 指针的指针（二级指针）
- [ ] 指针运算（+、-、++、--）
- [ ] 画出内存布局图来理解

### Day 3-4：指针与数组
- [ ] 数组名是指针常量
- [ ] 指针与数组的关系：`arr[i]` ≡ `*(arr+i)`
- [ ] 指针数组 vs 数组指针：
  ```c
  int *arr[10];    // 指针数组：10个指针
  int (*arr)[10];  // 数组指针：指向有10个int的数组
  ```
- [ ] 多维数组与指针
- [ ] 字符串（字符数组 vs 字符指针）

### Day 5-7：指针高级话题
- [ ] 函数指针：
  ```c
  int (*func_ptr)(int, int);  // 指向函数的指针
  // 用途：回调函数、驱动中的 file_operations 结构体
  ```
- [ ] void 指针 (`void *`) 及其用途
- [ ] const 与指针：
  ```c
  const int *p;     // 指向常量的指针
  int * const p;    // 常量指针
  const int * const p; // 两者都是常量
  ```
- [ ] `restrict` 关键字
- [ ] 实现一个简单的回调机制（如事件处理）

### 📝 第1周练习
1. 用二级指针实现动态二维数组
2. 实现一个函数指针驱动的简单计算器
3. 手写 `strcpy`, `strlen`, `strcmp` 等字符串函数
4. 画内存图解释程序的每一步

---

## 📖 第2周：内存管理

### Day 8-10：动态内存
- [ ] 堆 vs 栈 vs 静态区 vs 代码段（四区模型）
  ```
  高地址
  ┌─────────┐
  │  栈(stack) │  局部变量，向下增长
  ├─────────┤
  │   ↓     │
  │  空闲区   │
  │   ↑     │
  ├─────────┤
  │  堆(heap)  │  malloc分配，向上增长
  ├─────────┤
  │  BSS段    │  未初始化全局变量
  ├─────────┤
  │  Data段   │  已初始化全局变量
  ├─────────┤
  │  Text段   │  代码
  └─────────┘
  低地址
  ```
- [ ] malloc / calloc / realloc 使用
- [ ] free 之后置 NULL 的习惯
- [ ] 用 valgrind 检测内存泄漏：
  ```bash
  gcc -g program.c -o program
  valgrind --leak-check=full ./program
  ```

### Day 11-12：常见内存问题
- [ ] 内存泄漏（忘记 free）
- [ ] 野指针（使用已释放的内存）
- [ ] 缓冲区溢出
- [ ] 栈溢出（递归过深、大数组）
- [ ] 双重释放 (double free)
- [ ] 每个问题都写代码故意触发，观察现象

### Day 13-14：结构体与内存对齐
- [ ] 结构体定义与使用
- [ ] 内存对齐规则（重要！嵌入式结构体与硬件交互时）
- [ ] `#pragma pack` 和 `__attribute__((packed))`
- [ ] 联合体 (union) — 寄存器访问常用
- [ ] 位域 (bit-field) — 状态标志位
- [ ] `sizeof` 深入理解

### 📝 第2周练习
1. 实现一个简单的内存池 (memory pool)
2. 写一个动态数组（自动扩容的 vector）
3. 分析一个神秘 crash 的原因并修复

---

## 📖 第3周：数据结构（用C实现）

### Day 15-17：链表
- [ ] 单向链表（插入、删除、查找、反转）
- [ ] 双向链表 — **Linux 内核链表必学！**
- [ ] Linux 内核链表实现（`list.h` 核心思想）
  ```c
  // 内核链表的精妙之处：list_head 嵌入到自己的结构体中
  struct my_struct {
      int data;
      struct list_head list;  // 链表节点
  };
  // 通过 list_entry / container_of 宏获取外层结构体
  ```
- [ ] 理解 `container_of` 宏的实现原理

### Day 18-19：队列与栈
- [ ] 栈的实现（数组/链表）
- [ ] 普通队列、循环队列
- [ ] 了解 Linux 内核中的 kfifo（高效循环队列）

### Day 20-21：树与哈希
- [ ] 二叉树（插入、遍历：前中后序）
- [ ] 二叉搜索树
- [ ] 了解红黑树（内核中大量使用）
- [ ] 简单哈希表实现
- [ ] 了解内核中的 radix tree（页缓存）和 rb-tree（CFS调度器）

### 📝 第3周练习
1. 用内核链表风格实现一个通用链表库
2. 用循环队列实现一个生产者-消费者模型
3. 实现一个简单的符号表（用哈希表）

---

## 📖 第4周：位操作与底层编程

### Day 22-24：位操作（嵌入式核心技能）
- [ ] 位运算符：`&`, `|`, `^`, `~`, `<<`, `>>`
- [ ] 置位、清零、取反、测试：
  ```c
  #define SET_BIT(reg, n)    ((reg) |= (1U << (n)))
  #define CLEAR_BIT(reg, n)  ((reg) &= ~(1U << (n)))
  #define TOGGLE_BIT(reg, n) ((reg) ^= (1U << (n)))
  #define TEST_BIT(reg, n)   (((reg) >> (n)) & 1U)
  ```
- [ ] 掩码操作
- [ ] 大端 vs 小端（字节序）
- [ ] 判断系统字节序的代码

### Day 25-26：文件I/O（Linux系统调用）
- [ ] 理解文件描述符 (fd)
- [ ] 系统调用：`open`, `read`, `write`, `close`, `lseek`
- [ ] 标准C库 vs 系统调用的区别（带缓冲 vs 不带缓冲）
- [ ] `ioctl` — 设备控制接口
- [ ] `mmap` — 内存映射（嵌入式常用）

### Day 27-28：GDB 调试精通
- [ ] 编译时加 `-g` 选项
- [ ] 基本命令：
  ```
  run / r         运行程序
  break / b       设置断点
  next / n        单步（不进入函数）
  step / s        单步（进入函数）
  continue / c    继续运行
  print / p       打印变量
  bt              查看调用栈（backtrace）
  frame           切换栈帧
  watch           监视变量变化
  examine / x     查看内存
  ```
- [ ] 调试段错误 (Segmentation Fault)
- [ ] core dump 分析
- [ ] 调试多进程程序

### 📝 第4周练习
1. 写一个位图 (bitmap) 数据结构
2. 实现一个跨平台的字节序转换工具
3. 故意写一个有 bug 的程序，用 GDB 定位并修复

---

## 📖 第5-6周：工具链与大型项目

### Day 29-32：编译链接深入
- [ ] 理解 ELF 文件格式（粗略了解头部、段表）
- [ ] 符号表（symbol table）
- [ ] 静态链接 vs 动态链接
- [ ] 链接脚本 (linker script) 基础 — **嵌入式必学**
  ```ld
  SECTIONS {
      . = 0x80000000;     /* 起始地址 */
      .text : { *(.text) }
      .data : { *(.data) }
      .bss  : { *(.bss)  }
  }
  ```
- [ ] 使用 `objdump`, `readelf`, `nm` 分析目标文件

### Day 33-36：项目构建系统
- [ ] Makefile 进阶（函数、条件、嵌套）
- [ ] CMake 入门（现代 C/C++ 构建）
- [ ] 了解 Autotools（很多开源项目用）

### Day 37-40：代码质量
- [ ] 编码规范（Linux 内核编码风格）
- [ ] 静态分析：`cppcheck`
- [ ] 防御性编程
- [ ] 错误处理模式（errno, 返回值, goto cleanup）
- [ ] 单元测试：了解 Unity/Ceedling（嵌入式测试框架）

### Day 41-42：模块化设计
- [ ] 设计一个可移植的 HAL（硬件抽象层）
- [ ] 理解回调、注册机制
- [ ] 状态机编程（嵌入式必备模式）

### 📝 第5-6周练习：阶段二综合项目
**项目：简易命令解析器 (Shell)**
- 支持内置命令（cd, pwd, exit, help）
- 支持外部命令执行（fork + exec）
- 支持管道 `|` 和重定向 `>`, `<`
- 支持后台运行 `&`
- 代码模块化设计，Makefile 构建
- 用 GDB 调试所有功能

---

## ✅ 阶段二检查清单

- [ ] 能徒手写出带内核链表的数据结构
- [ ] 能解释指针所有用法，不惧怕多重指针
- [ ] 能用 valgrind 找到内存泄漏并修复
- [ ] 能熟练使用 GDB 定位 crash 问题
- [ ] 理解 ELF 文件基本结构和链接过程
- [ ] 能写出规范、可维护的 C 代码
- [ ] 能看懂简单链接脚本

---

## 📚 阶段二推荐资源

- **必读：** 《C 和指针》(Kenneth Reek)
- **必读：** 《C 专家编程》(Peter van der Linden)
- **参考：** 《深入理解计算机系统》(CS:APP) — 第3、7、9章
- **参考：** Linux 内核 `include/linux/list.h` 源码
- **练习：** [LeetCode C语言题](https://leetcode.com/) 用 C 刷 30 道数据结构题
